[pytest]
testpaths =
    controller/tests
    compiler/tests
pythonpath =
    controller
    compiler
addopts = --import-mode=importlib
markers =
    runtime_integration: test uses real local runtime processes and requires an isolated environment
