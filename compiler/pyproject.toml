[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "elements-compiler"
version = "0.1.0"
description = "Elements v2 LED animation compiler"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
